//An entertainment-based command to provide players with missions and challenges to complete, with no material rewards.

var Command = require("../command");
var Interface = require("../interface");

module.exports = {
    commands: []
}